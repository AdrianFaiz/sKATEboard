window.onload = () => {
  document.body.classList.remove("container"); // Your existing code

  // Play the audio automatically as the page loads
  const music = document.getElementById("music1");
  music.play(); // Start playing the music
};
